# 2048 小游戏 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a 5×5 infinite-mode 2048 game with scoreboard and undo Easter egg, deployable on GitHub Pages.

**Architecture:** Pure HTML/CSS/JS single-page app. Game state in a JS object (2D array), rendered to DOM via a render function. localStorage for persistence. No frameworks, no build tools.

**Tech Stack:** HTML5, CSS3, Vanilla JavaScript, localStorage

---

### Task 1: Project scaffolding and HTML structure

**Files:**
- Create: `D:\项目\small game\index.html`
- Create: `D:\项目\small game\style.css`
- Create: `D:\项目\small game\script.js`
- Create: `D:\项目\small game\README.md`

- [ ] **Step 1: Create `index.html` with full game layout**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>2048</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div id="game-container">
        <h1 class="title">2048</h1>
        <div class="scoreboard">
            <div class="score-box">
                <span class="score-label">分数</span>
                <span id="score" class="score-value">0</span>
            </div>
            <div class="score-box">
                <span class="score-label">最高</span>
                <span id="best-score" class="score-value">0</span>
            </div>
            <div class="score-box">
                <span class="score-label">最大</span>
                <span id="max-tile" class="score-value">0</span>
            </div>
        </div>
        <div class="controls">
            <button id="new-game-btn" class="btn">🔄 新游戏</button>
            <button id="undo-btn" class="btn">💬 撤销</button>
        </div>
        <div id="board" class="board">
            <!-- tiles rendered by JS -->
        </div>
        <p class="hint">使用方向键 ← ↑ → ↓ 或滑动操作</p>
    </div>
    <div id="game-over-overlay" class="overlay hidden">
        <div class="overlay-content">
            <h2>游戏结束</h2>
            <p id="final-score">得分: 0</p>
            <button id="restart-btn" class="btn">再来一局</button>
        </div>
    </div>
    <script src="script.js"></script>
</body>
</html>
```

- [ ] **Step 2: Create placeholder `style.css`**

```css
*, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Segoe UI', 'Arial', sans-serif;
    background: #faf8ef;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    min-height: 100vh;
    padding-top: 20px;
    -webkit-tap-highlight-color: transparent;
    user-select: none;
}

#game-container {
    width: 100%;
    max-width: 500px;
    padding: 0 15px;
    text-align: center;
}

.title {
    font-size: 3.5rem;
    font-weight: 800;
    color: #776e65;
    margin-bottom: 10px;
    letter-spacing: 8px;
}

.scoreboard {
    display: flex;
    justify-content: center;
    gap: 10px;
    margin-bottom: 10px;
}

.score-box {
    background: #bbada0;
    border-radius: 6px;
    padding: 8px 16px;
    min-width: 80px;
    text-align: center;
}

.score-label {
    display: block;
    font-size: 0.7rem;
    color: #eee4da;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.score-value {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    color: #fff;
}

.controls {
    display: flex;
    justify-content: center;
    gap: 10px;
    margin-bottom: 15px;
}

.btn {
    background: #8f7a66;
    color: #f9f6f2;
    border: none;
    border-radius: 6px;
    padding: 10px 20px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s;
}

.btn:hover {
    background: #9f8b77;
}

.btn:active {
    transform: scale(0.97);
}

.board {
    background: #bbada0;
    border-radius: 8px;
    padding: 10px;
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 8px;
    aspect-ratio: 1;
    position: relative;
    margin: 0 auto;
}

.tile {
    background: rgba(238, 228, 218, 0.35);
    border-radius: 6px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.8rem;
    font-weight: 700;
    color: #776e65;
    aspect-ratio: 1;
    transition: none;
}

.tile-new {
    animation: tile-pop 0.2s ease;
}

.tile-merged {
    animation: tile-merge 0.2s ease;
}

@keyframes tile-pop {
    0% { transform: scale(0); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
}

@keyframes tile-merge {
    0% { transform: scale(1); }
    50% { transform: scale(1.2); }
    100% { transform: scale(1); }
}

.hint {
    margin-top: 15px;
    color: #776e65;
    font-size: 0.85rem;
    opacity: 0.7;
}

.overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 100;
}

.overlay-content {
    background: #faf8ef;
    border-radius: 12px;
    padding: 40px;
    text-align: center;
}

.overlay-content h2 {
    font-size: 2rem;
    color: #776e65;
    margin-bottom: 10px;
}

.overlay-content p {
    font-size: 1.2rem;
    color: #776e65;
    margin-bottom: 20px;
}

.hidden {
    display: none !important;
}

/* Tile colors */
.tile-2    { background: #eee4da; color: #776e65; }
.tile-4    { background: #ede0c8; color: #776e65; }
.tile-8    { background: #f2b179; color: #f9f6f2; }
.tile-16   { background: #f59563; color: #f9f6f2; }
.tile-32   { background: #f67c5f; color: #f9f6f2; }
.tile-64   { background: #f65e3b; color: #f9f6f2; }
.tile-128  { background: #edcf72; color: #f9f6f2; font-size: 1.6rem; }
.tile-256  { background: #edcc61; color: #f9f6f2; font-size: 1.6rem; }
.tile-512  { background: #edc850; color: #f9f6f2; font-size: 1.6rem; }
.tile-1024 { background: #edc53f; color: #f9f6f2; font-size: 1.3rem; }
.tile-2048 { background: #edc22e; color: #f9f6f2; font-size: 1.3rem; }
.tile-4096 { background: #3c3a32; color: #f9f6f2; font-size: 1.3rem; }
.tile-8192 { background: #3c3a32; color: #f9f6f2; font-size: 1.2rem; }
.tile-super { background: #3c3a32; color: #f9f6f2; font-size: 1rem; }

/* Responsive */
@media (max-width: 520px) {
    .title { font-size: 2.5rem; letter-spacing: 5px; }
    .board { padding: 6px; gap: 6px; }
    .tile { font-size: 1.3rem; }
    .tile-128, .tile-256, .tile-512 { font-size: 1.1rem; }
    .tile-1024, .tile-2048 { font-size: 1rem; }
    .tile-4096, .tile-8192, .tile-super { font-size: 0.85rem; }
    .score-box { min-width: 60px; padding: 5px 10px; }
    .score-value { font-size: 1.2rem; }
    .btn { padding: 8px 14px; font-size: 0.85rem; }
}

@media (max-width: 360px) {
    .title { font-size: 2rem; }
    .board { padding: 4px; gap: 4px; }
    .tile { font-size: 1rem; }
    .tile-128, .tile-256, .tile-512 { font-size: 0.9rem; }
    .tile-1024, .tile-2048, .tile-4096, .tile-8192, .tile-super { font-size: 0.75rem; }
}
```

- [ ] **Step 3: Create `README.md`**

```markdown
# 2048 - 无限模式

5×5 棋盘 · 无限挑战 · 生活哲学

在线游玩: [GitHub Pages 链接待部署]

## 操作方式

- **桌面**: 方向键 ← ↑ → ↓
- **手机**: 滑动屏幕
- **按钮**: 新游戏 / ~~撤销~~（生活还在继续，请继续向前走吧！！）

## 本地运行

直接用浏览器打开 `index.html` 即可游玩。

## 部署到 GitHub Pages

1. 创建 GitHub 仓库
2. 将文件推送到 `main` 分支
3. 在仓库 Settings → Pages 中，选择 Deploy from main
4. 访问 `https://<用户名>.github.io/<仓库名>/`
```

- [ ] **Step 4: Commit scaffolding**

```bash
git init
git add -A
git commit -m "feat: project scaffolding with HTML structure and styles"
```

---

### Task 2: Core game logic

**Files:**
- Modify: `D:\项目\small game\script.js` (full game state management)

- [ ] **Step 1: Write the Game state object and initialization logic in `script.js`**

```javascript
const GRID_SIZE = 5;
const WIN_VALUE = null; // infinite mode — no win condition

let grid = [];
let score = 0;
let bestScore = 0;
let maxTile = 0;
let gameOver = false;

// Initialize empty grid
function createEmptyGrid() {
    return Array.from({ length: GRID_SIZE }, () =>
        Array.from({ length: GRID_SIZE }, () => 0)
    );
}

// Get all empty cell positions
function getEmptyCells(g) {
    const cells = [];
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            if (g[r][c] === 0) cells.push({ r, c });
        }
    }
    return cells;
}

// Add a random tile (90% chance 2, 10% chance 4)
function addRandomTile(g) {
    const empty = getEmptyCells(g);
    if (empty.length === 0) return null;
    const { r, c } = empty[Math.floor(Math.random() * empty.length)];
    g[r][c] = Math.random() < 0.9 ? 2 : 4;
    return { r, c, value: g[r][c] };
}

// Initialize new game
function initGame() {
    grid = createEmptyGrid();
    score = 0;
    gameOver = false;
    addRandomTile(grid);
    addRandomTile(grid);
    updateMaxTile();
    render();
    updateScoreboard();
    hideGameOver();
}
```

- [ ] **Step 2: Implement core movement logic (slide and merge)**

```javascript
// Slide a single row to the left and merge
function slideRow(row) {
    // Remove zeros
    let tiles = row.filter(v => v !== 0);
    let merged = [];
    let i = 0;
    
    while (i < tiles.length) {
        if (i + 1 < tiles.length && tiles[i] === tiles[i + 1]) {
            const mergedValue = tiles[i] * 2;
            merged.push(mergedValue);
            score += mergedValue;
            i += 2;
        } else {
            merged.push(tiles[i]);
            i++;
        }
    }
    
    // Pad with zeros
    while (merged.length < GRID_SIZE) {
        merged.push(0);
    }
    
    return merged;
}

// Check if row changed after sliding
function rowsEqual(a, b) {
    return a.every((v, i) => v === b[i]);
}

function gridsEqual(a, b) {
    for (let r = 0; r < GRID_SIZE; r++) {
        if (!rowsEqual(a[r], b[r])) return false;
    }
    return true;
}

// Move all tiles in a direction
function move(direction) {
    if (gameOver) return;
    
    const previous = grid.map(row => [...row]);
    const prevScore = score;
    
    // Rotate grid based on direction so we always slide left
    let rotated = false;
    let gridToSlide = grid.map(row => [...row]);
    
    if (direction === 'up') {
        gridToSlide = rotateGrid(gridToSlide, 1); // rotate 90° CCW to slide left
        rotated = true;
    } else if (direction === 'down') {
        gridToSlide = rotateGrid(gridToSlide, -1); // rotate 90° CW to slide left
        rotated = true;
    } else if (direction === 'right') {
        gridToSlide = gridToSlide.map(row => [...row].reverse());
        rotated = true;
    }
    // left: no transformation needed
    
    // Slide each row
    const newGrid = gridToSlide.map(row => slideRow(row));
    
    // Reverse transformations
    let result;
    if (direction === 'up') {
        result = rotateGrid(newGrid, -1);
    } else if (direction === 'down') {
        result = rotateGrid(newGrid, 1);
    } else if (direction === 'right') {
        result = newGrid.map(row => [...row].reverse());
    } else {
        result = newGrid;
    }
    
    // Check if anything changed
    if (gridsEqual(grid, result)) return;
    
    grid = result;
    updateMaxTile();
    addRandomTile(grid);
    updateScoreboard();
    render();
    
    if (isGameOver()) {
        gameOver = true;
        showGameOver();
    }
}

// Rotate grid 90°: dir=1 for CCW, dir=-1 for CW
function rotateGrid(g, dir) {
    const n = g.length;
    const rotated = createEmptyGrid();
    for (let r = 0; r < n; r++) {
        for (let c = 0; c < n; c++) {
            if (dir === 1) {
                rotated[n - 1 - c][r] = g[r][c];
            } else {
                rotated[c][n - 1 - r] = g[r][c];
            }
        }
    }
    return rotated;
}
```

- [ ] **Step 3: Implement game-over detection, max tile, and score persistence**

```javascript
// Check if game is over
function isGameOver() {
    // Check for empty cells
    if (getEmptyCells(grid).length > 0) return false;
    
    // Check for adjacent equal tiles
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            const val = grid[r][c];
            if (c + 1 < GRID_SIZE && grid[r][c + 1] === val) return false;
            if (r + 1 < GRID_SIZE && grid[r + 1][c] === val) return false;
        }
    }
    return true;
}

// Update max tile value
function updateMaxTile() {
    let max = 0;
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            if (grid[r][c] > max) max = grid[r][c];
        }
    }
    maxTile = max;
}

// Best score persistence
function saveBestScore() {
    if (score > bestScore) {
        bestScore = score;
        localStorage.setItem('2048-best-score', bestScore.toString());
    }
}

function loadBestScore() {
    const saved = localStorage.getItem('2048-best-score');
    bestScore = saved ? parseInt(saved, 10) : 0;
}

// Save / load game state
function saveGame() {
    const state = { grid, score, maxTile };
    try {
        localStorage.setItem('2048-game-state', JSON.stringify(state));
    } catch (e) {
        // localStorage full or unavailable — silently ignore
    }
}

function loadGame() {
    try {
        const saved = localStorage.getItem('2048-game-state');
        if (!saved) return false;
        const state = JSON.parse(saved);
        if (!state.grid || state.grid.length !== GRID_SIZE) return false;
        grid = state.grid;
        score = state.score || 0;
        maxTile = state.maxTile || 0;
        return true;
    } catch (e) {
        return false;
    }
}

function clearSavedGame() {
    localStorage.removeItem('2048-game-state');
}
```

- [ ] **Step 4: Commit game logic**

```bash
git add script.js
git commit -m "feat: core game logic — grid, movement, merging, game-over detection"
```

---

### Task 3: Rendering and DOM interaction

**Files:**
- Modify: `D:\项目\small game\script.js` (render functions, event bindings, game-over overlay)

- [ ] **Step 1: Write render function and DOM element creation**

```javascript
const boardEl = document.getElementById('board');
const scoreEl = document.getElementById('score');
const bestScoreEl = document.getElementById('best-score');
const maxTileEl = document.getElementById('max-tile');
const gameOverOverlay = document.getElementById('game-over-overlay');
const finalScoreEl = document.getElementById('final-score');

// Render the grid to the DOM
function render() {
    boardEl.innerHTML = '';
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            const tile = document.createElement('div');
            tile.className = 'tile';
            const value = grid[r][c];
            if (value !== 0) {
                const tileClass = getTileClass(value);
                tile.classList.add(tileClass);
                tile.textContent = value;
            }
            boardEl.appendChild(tile);
        }
    }
}

function getTileClass(value) {
    if (value <= 8192) return 'tile-' + value;
    return 'tile-super';
}

function updateScoreboard() {
    scoreEl.textContent = score;
    if (score > bestScore) {
        bestScore = score;
        localStorage.setItem('2048-best-score', bestScore.toString());
    }
    bestScoreEl.textContent = bestScore;
    maxTileEl.textContent = maxTile;
    // Auto-save after every move
    saveGame();
}

function showGameOver() {
    gameOverOverlay.classList.remove('hidden');
    finalScoreEl.textContent = '得分: ' + score;
    saveBestScore();
    clearSavedGame();
}

function hideGameOver() {
    gameOverOverlay.classList.add('hidden');
}
```

- [ ] **Step 2: Wire up keyboard, touch controls, and button handlers**

```javascript
// Keyboard controls
document.addEventListener('keydown', (e) => {
    const keyMap = {
        'ArrowUp': 'up',
        'ArrowDown': 'down',
        'ArrowLeft': 'left',
        'ArrowRight': 'right'
    };
    const direction = keyMap[e.key];
    if (direction) {
        e.preventDefault();
        move(direction);
    }
});

// Touch controls
let touchStartX = 0;
let touchStartY = 0;

document.addEventListener('touchstart', (e) => {
    touchStartX = e.touches[0].clientX;
    touchStartY = e.touches[0].clientY;
}, { passive: true });

document.addEventListener('touchend', (e) => {
    const dx = e.changedTouches[0].clientX - touchStartX;
    const dy = e.changedTouches[0].clientY - touchStartY;
    const absDx = Math.abs(dx);
    const absDy = Math.abs(dy);
    
    // Minimum swipe distance: 30px
    if (Math.max(absDx, absDy) < 30) return;
    
    if (absDx > absDy) {
        move(dx > 0 ? 'right' : 'left');
    } else {
        move(dy > 0 ? 'down' : 'up');
    }
}, { passive: true });

// Button handlers
document.getElementById('new-game-btn').addEventListener('click', () => {
    clearSavedGame();
    initGame();
});

document.getElementById('restart-btn').addEventListener('click', () => {
    clearSavedGame();
    initGame();
});

document.getElementById('undo-btn').addEventListener('click', () => {
    // Easter egg — no real undo
    alert('生活还在继续，请继续向前走吧！！');
});
```

- [ ] **Step 3: Initialize on page load (with saved state restore)**

```javascript
// On load: restore saved game or start fresh
loadBestScore();

if (loadGame()) {
    // Verify saved grid is valid
    if (isGameOver()) {
        clearSavedGame();
        initGame();
    } else {
        updateScoreboard();
        render();
        if (isGameOver()) {
            gameOver = true;
            showGameOver();
        }
    }
} else {
    initGame();
}
```

- [ ] **Step 4: Commit rendering and controls**

```bash
git add script.js
git commit -m "feat: rendering, keyboard/touch controls, and button handlers"
```

---

### Task 4: Add tile animation support

**Files:**
- Modify: `D:\项目\small game\script.js` (track new/merged tiles for animation)
- Modify: `D:\项目\small game\style.css` (animation classes already defined)

- [ ] **Step 1: Add tracking of newly spawned and merged tiles**

Add tracking variables at the top of script.js:
```javascript
let newTilePos = null;   // { r, c } of the tile added after a move
let mergedPositions = []; // [{ r, c }] of tiles that just merged
```

Modify `addRandomTile` to record position:
```javascript
function addRandomTile(g) {
    const empty = getEmptyCells(g);
    if (empty.length === 0) return null;
    const { r, c } = empty[Math.floor(Math.random() * empty.length)];
    g[r][c] = Math.random() < 0.9 ? 2 : 4;
    newTilePos = { r, c };
    return { r, c, value: g[r][c] };
}
```

Modify `slideRow` to track merges (need a row-level index):
Because slideRow currently returns only values, we need to track which positions merged. Change approach — compute merged positions from the grid before/after:
```javascript
// After slideRow processes, determine which tiles grew (merged)
function findMergedTiles(before, after) {
    const merged = [];
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            if (after[r][c] !== 0 && after[r][c] !== before[r][c]) {
                // This tile value changed — either merged or was already growing
                // Check if the tile before had half the value and an adjacent match
                if (before[r][c] === after[r][c] / 2) {
                    // Find a neighbor that also halved
                    const halfVal = after[r][c] / 2;
                    const neighbors = [
                        [r-1,c],[r+1,c],[r,c-1],[r,c+1]
                    ];
                    for (const [nr, nc] of neighbors) {
                        if (nr >= 0 && nr < GRID_SIZE && nc >= 0 && nc < GRID_SIZE
                            && before[nr][nc] === halfVal && after[nr][nc] === 0) {
                            merged.push({ r, c });
                            break;
                        }
                    }
                }
            }
        }
    }
    return merged;
}
```

Reset tracking at start of move and detect after:
In `move()`, before slide: `mergedPositions = []; newTilePos = null;`
After slide: `mergedPositions = findMergedTiles(previous, grid);`

- [ ] **Step 2: Add animation CSS classes in render**

Modify the render function to add animation classes:
```javascript
function render() {
    boardEl.innerHTML = '';
    const idx = (r, c) => r * GRID_SIZE + c;
    for (let r = 0; r < GRID_SIZE; r++) {
        for (let c = 0; c < GRID_SIZE; c++) {
            const tile = document.createElement('div');
            tile.className = 'tile';
            const value = grid[r][c];
            if (value !== 0) {
                tile.classList.add(getTileClass(value));
                tile.textContent = value;
                
                // Animate new tiles
                if (newTilePos && newTilePos.r === r && newTilePos.c === c) {
                    tile.classList.add('tile-new');
                }
                
                // Animate merged tiles
                if (mergedPositions.some(p => p.r === r && p.c === c)) {
                    tile.classList.add('tile-merged');
                }
            }
            boardEl.appendChild(tile);
        }
    }
}
```

- [ ] **Step 3: Commit animations**

```bash
git add script.js style.css
git commit -m "feat: tile spawn and merge animations"
```

---

### Task 5: Final integration and README

**Files:**
- Modify: `D:\项目\small game\README.md`
- Modify: `D:\项目\small game\index.html`

- [ ] **Step 1: Final README with deployment instructions and link**

(Already created in Task 1 — update if needed after implementation.)

- [ ] **Step 2: Final test — open `index.html` in browser and verify:**

1. 5×5 grid renders with 2 initial tiles
2. Arrow keys move tiles correctly
3. Merging works (two 2s → 4, score updates)
4. Touch swipe works on mobile viewport
5. Scoreboard updates in real-time
6. Max tile value displays correctly
7. Game-over overlay appears when board is full
8. "新游戏" resets everything
9. "撤销" shows the Easter egg message
10. Page refresh restores game state
11. "再来一局" in game-over works

- [ ] **Step 3: Final commit**

```bash
git add -A
git commit -m "feat: final integration — 2048 infinite mode complete"
```

---

## Spec Coverage Check

| Spec Requirement | Task |
|-----------------|------|
| 5×5 grid | Task 2 (GRID_SIZE = 5) |
| Infinite mode (no win) | Task 2 (WIN_VALUE = null) |
| Score board (current + best + max) | Task 3 (updateScoreboard) |
| Best score persistence (localStorage) | Task 2 (saveBestScore/loadBestScore) |
| Game state save/load | Task 2 (saveGame/loadGame) |
| Keyboard controls | Task 3 (keydown listener) |
| Touch controls | Task 3 (touchstart/touchend) |
| Tile animation | Task 4 (pop + merge animations) |
| Game over detection | Task 2 (isGameOver) |
| New Game button | Task 3 (handler) |
| Undo Easter egg | Task 3 (alert message) |
| Responsive design | Task 1 (CSS media queries) |
| README + deploy docs | Task 1 + Task 5 |
